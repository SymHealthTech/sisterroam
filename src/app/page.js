// The homepage lives in (public)/page.js.
// This file must be deleted (or this project moved to use only one route group)
// to avoid the "Two pages resolve to the same URL" Next.js build error.
// See: src/app/(public)/page.js

export { default, metadata } from './(public)/page'
